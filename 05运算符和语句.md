# 💯💯💯 运算符

> ## 🧠 总结表格

| 分类     | 运算符               | 功能                         |
| -------- | -------------------- | ---------------------------- | --- | ------------ |
| 算术     | +, -, \*, /, ~/ , %  | 基本数学计算                 |
| 关系     | ==, !=, >, <, >=, <= | 比较值关系                   |
| 逻辑     | !, &&,               |                              |     | 布尔逻辑判断 |
| 空感知   | ?., ??, ??=          | 防止 null 报错，设置默认值   |
| 级联     | ..                   | 多次访问同一对象的属性或方法 |
| 类型测试 | is, is!              | 判断对象类型                 |

> ## 一、算术运算符

| 运算符 | 说明                  |
| ------ | --------------------- |
| `+`    | 加法                  |
| `-`    | 减法                  |
| `*`    | 乘法                  |
| `/`    | 除法（结果是 double） |
| `~/`   | 整除（结果是 int）    |
| `%`    | 取模（余数）          |

### 🌰 经典示例：

```dart
void main() {
  int a = 10;
  int b = 3;

  print(a + b);   // 加法：13
  print(a - b);   // 减法：7
  print(a * b);   // 乘法：30
  print(a / b);   // 除法（结果是double）：3.3333333333333335
  print(a ~/ b);  // 整除：3
  print(a % b);   // 取模（余数）：1
}
```

💡 **技巧：**

- `/` 返回 `double` 类型；
- `~/` 才是“真正意义的整除”。

---

> ## 二、关系和相等运算符

| 运算符 | 说明           |
| ------ | -------------- |
| `==`   | 判断是否相等   |
| `!=`   | 判断是否不相等 |
| `>`    | 大于           |
| `<`    | 小于           |
| `>=`   | 大于等于       |
| `<=`   | 小于等于       |

### 🌰 示例：

```dart
void main() {
  int a = 5;
  int b = 10;

  print(a == b);  // false
  print(a != b);  // true
  print(a > b);   // false
  print(a < b);   // true
  print(a >= 5);  // true
  print(b <= 10); // true
}
```

---

> ## 三、逻辑运算符

| 运算符 | 说明                            |     |     |
| ------ | ------------------------------- | --- | --- |
| `!`    | 逻辑非（取反）                  |     |     |
| `&&`   | 逻辑与（同时为 true 才是 true） |     |     |
| `\|\|` | 逻辑或（任一为 true 即为 true） |

### 🌰 示例：

```dart
void main() {
  bool a = true;
  bool b = false;

  print(!a);       // false
  print(a && b);   // false
  print(a || b);   // true
}
```

💡 常用于条件判断，例如：

```dart
if (userLoggedIn && hasPermission) {
  print('允许访问');
}
```

---

> ## 四、空感知运算符（重点 ⭐️）

| 运算符 | 说明                                          |
| ------ | --------------------------------------------- |
| `?.`   | 条件成员访问（如果对象为 null，就不会抛异常） |
| `??`   | 空合并运算符（如果左边为 null，返回右边）     |
| `??=`  | 空赋值运算符（如果变量为 null，才赋值）       |

### 🌰 示例 1：`?.`（防止空指针异常）

```dart
class User {
  String? name;
}

void main() {
  User? user; // 当前 user 是 null
  print(user?.name); // 不会报错，结果是 null（而不是抛异常）
}
```

### 🌰 示例 2：`??`

```dart
void main() {
  String? username;
  // 如果 username 是 null，就用 "游客" 替代
  print(username ?? "游客"); // 输出：游客
}
```

### 🌰 示例 3：`??=`

```dart
void main() {
  String? name;
  name ??= "默认用户名"; // 只有当 name 为 null 时才赋值
  print(name); // 输出：默认用户名
}
```

💡 **这三个运算符几乎每天都在 Flutter 里用**，例如：

```dart
Text(user?.name ?? '未知用户')
```

---

> ## 五、级联运算符 `..`

**让多个操作链式执行，而不需要重复对象名**

### 🌰 示例：

```dart
class Person {
  String? name;
  int? age;

  void sayHello() {
    print("你好，我是 $name，今年 $age 岁");
  }
}

void main() {
  // 普通写法
  var p = Person();
  p.name = '小明';
  p.age = 20;
  p.sayHello();

  // 使用级联运算符
  var p2 = Person()
    ..name = '小红'
    ..age = 18
    ..sayHello();  // 输出：你好，我是 小红，今年 18 岁
}
```

💡 相当于：

```dart
p2.name = '小红';
p2.age = 18;
p2.sayHello();
```

但写法更简洁、更优雅。

---

> ## 六、类型测试运算符：`is` / `is!`

| 运算符 | 说明                             |
| ------ | -------------------------------- |
| `is`   | 判断对象是否属于某个类型         |
| `is!`  | 判断对象是否 **不** 属于某个类型 |

### 🌰 示例：

```dart
void main() {
  var num1 = 42;
  var str = "Hello";

  print(num1 is int);     // true
  print(num1 is String);  // false
  print(str is! int);     // true
}
```

💡 常用于类型判断（尤其在 Flutter 组件或数据模型中）：

```dart
void printType(Object obj) {
  if (obj is String) {
    print("是字符串");
  } else if (obj is int) {
    print("是整数");
  } else {
    print("其他类型");
  }
}
```

---

# 💯💯💯 Dart 条件与循环语句大全

> ## 🧠 总结表格

| 分类       | 关键字                      | 功能                   |
| ---------- | --------------------------- | ---------------------- |
| 条件语句   | `if`, `else if`, `else`     | 判断条件执行不同分支   |
| 多分支     | `switch`, `case`, `default` | 多个固定值匹配         |
| 计数循环   | `for`                       | 按次数循环执行         |
| 集合循环   | `for...in`                  | 遍历列表、Set、Map     |
| 条件循环   | `while`                     | 条件为真则持续执行     |
| 后判断循环 | `do...while`                | 先执行一次再判断       |
| 跳出循环   | `break`                     | 立即退出整个循环       |
| 跳过一次   | `continue`                  | 跳过当前循环继续下一次 |

> ## ✅ `if`、`else if`、`else`

### 🌰 经典案例：判断成绩等级

```dart
void main() {
  int score = 85;

  // 基本的 if 语句
  if (score >= 90) {
    print("优秀");
  }
  // else if 表示当上面条件不满足时，尝试另一个条件
  else if (score >= 80) {
    print("良好");
  }
  else if (score >= 60) {
    print("及格");
  }
  // else 表示以上所有条件都不满足时执行
  else {
    print("不及格");
  }
}
```

🧠 **说明：**

- 条件必须是 `bool` 类型（true / false）。
- Dart 不支持像 JavaScript 那样的“非零即真”语法，必须写成明确的布尔表达式。

---

## ✅ `switch`、`case`

Dart 的 `switch` 比较独特：

- 支持 **字符串**、**枚举**、甚至 **对象**。
- `case` 后面的表达式必须是常量。
- 每个 `case` 语句块**必须以 `break`、`return` 或 `continue` 结束**。

---

### 🌰 示例 1：字符串判断

```dart
void main() {
  String role = "admin";

  switch (role) {
    case "admin":
      print("管理员权限");
      break;

    case "user":
      print("普通用户权限");
      break;

    case "guest":
      print("游客权限");
      break;

    default:
      print("未知身份");
  }
}
```

---

### 🌰 示例 2：枚举判断（Flutter 常用）

```dart
enum Day { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

void main() {
  var today = Day.saturday;

  switch (today) {
    case Day.saturday:
    case Day.sunday:
      print("周末休息");
      break;
    default:
      print("工作日");
  }
}
```

💡 `switch` 的多个 `case` 可以共用一个结果（如上）。

---

> ## ✅ `for` 循环（经典）

### 🌰 示例：打印 1 到 5

```dart
void main() {
  for (int i = 1; i <= 5; i++) {
    print("第 $i 次循环");
  }
}
```

🧠 **说明：**

- 三个部分分别是：

  1. 初始化：`int i = 1`
  2. 条件判断：`i <= 5`
  3. 迭代操作：`i++`

- 每执行一轮循环后，都会检查条件是否为真。

---

> ## ✅ `for...in` 循环（遍历集合）

### 🌰 示例：遍历列表

```dart
void main() {
  List<String> fruits = ["苹果", "香蕉", "橙子"];

  for (String fruit in fruits) {
    print("我喜欢吃 $fruit");
  }
}
```

💡 Flutter 中非常常见，例如遍历一个组件列表：

```dart
for (var item in items) Text(item)
```

---

> ## ✅ `while` 循环

### 🌰 示例：从 1 加到 5

```dart
void main() {
  int i = 1;

  while (i <= 5) {
    print("当前数字：$i");
    i++;
  }
}
```

🧠 **说明：**

- `while` 先判断条件，再执行代码块；
- 如果第一次条件为 `false`，循环体**一次都不会执行**。

---

> ## ✅ `do...while` 循环

### 🌰 示例：至少执行一次

```dart
void main() {
  int count = 0;

  do {
    print("执行第 ${count + 1} 次");
    count++;
  } while (count < 3);
}
```

🧠 **区别：**

- `do...while` 会**先执行一次循环体**，再判断条件；
- 所以即使一开始条件是 `false`，也至少执行一次。

---

> ## 三、控制循环的语句

---

### ✅ `break`：立即退出循环

```dart
void main() {
  for (int i = 1; i <= 10; i++) {
    if (i == 5) {
      print("到5就停止循环");
      break; // 直接结束整个循环
    }
    print(i);
  }
}
```

---

### ✅ `continue`：跳过当前循环，继续下一次

```dart
void main() {
  for (int i = 1; i <= 5; i++) {
    if (i == 3) {
      print("跳过 3");
      continue; // 跳过 3，继续下一个循环
    }
    print("数字：$i");
  }
}
```

---

# 💯💯💯 综合案例：循环 + 判断 + break + continue

### 🌰 示例：打印偶数，遇到 8 停止

```dart
void main() {
  for (int i = 1; i <= 10; i++) {
    if (i % 2 != 0) {
      continue; // 如果是奇数就跳过
    }

    if (i == 8) {
      print("遇到 8，停止循环！");
      break; // 提前结束
    }

    print("偶数：$i");
  }
}
```

输出结果：

```
偶数：2
偶数：4
偶数：6
遇到 8，停止循环！
```
