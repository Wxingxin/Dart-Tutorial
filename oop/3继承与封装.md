非常好 👍 我来给你讲解 **Dart 面向对象编程（OOP）中「继承」与「封装」的最基础知识点大全**，每个知识点我都配上**经典代码示例**和**讲解**，让你一步一步打牢基础。

---

## 🧱 一、封装（Encapsulation）

> **封装**是指将数据（属性）和操作这些数据的方法封装在类中，并通过**访问控制**来保护数据。

### 1️⃣ Dart 中的封装机制

* Dart 没有 `public`、`private`、`protected` 这些关键字。
* **私有成员**使用 **`_`（下划线）** 开头即可。

```dart
class Person {
  // 私有属性（只能在当前文件中访问）
  String _name;
  int _age;

  // 构造函数
  Person(this._name, this._age);

  // 公有方法（外部可以访问）
  void introduce() {
    print('My name is $_name, I am $_age years old.');
  }

  // Getter （让外部“读”私有属性）
  String get name => _name;

  // Setter （让外部“改”私有属性）
  set name(String value) {
    if (value.isEmpty) {
      print("Name can't be empty!");
    } else {
      _name = value;
    }
  }
}

void main() {
  var p = Person('Tom', 18);
  p.introduce(); // ✅ 输出：My name is Tom, I am 18 years old.

  print(p.name); // ✅ 通过 getter 获取

  p.name = '';   // ❌ Name can't be empty!
  p.name = 'Jerry';
  print(p.name); // ✅ Jerry
}
```

### ✅ 封装的要点总结：

| 特性            | Dart 表现    |
| ------------- | ---------- |
| 私有属性          | 用 `_` 开头   |
| Getter/Setter | 控制外部访问     |
| 数据保护          | 避免随意修改内部状态 |

---

## 🧬 二、继承（Inheritance）

> **继承**可以让子类复用父类的属性和方法，并可以扩展或重写父类的功能。

### 1️⃣ 基本继承语法

```dart
class Animal {
  String name;

  Animal(this.name);

  void eat() {
    print('$name is eating');
  }
}

class Dog extends Animal {
  // Dog 继承自 Animal
  Dog(String name) : super(name);

  void bark() {
    print('$name is barking');
  }
}

void main() {
  var dog = Dog('Buddy');
  dog.eat();  // ✅ Buddy is eating
  dog.bark(); // ✅ Buddy is barking
}
```

> 🔹 子类用 `extends` 关键字继承父类
> 🔹 用 `super` 调用父类构造函数

---

### 2️⃣ 方法重写（Override）

> 子类可以重写父类的方法，用 `@override` 标记。

```dart
class Animal {
  void speak() {
    print('Animal is making a sound');
  }
}

class Cat extends Animal {
  @override
  void speak() {
    print('Cat says: Meow!');
  }
}

void main() {
  var cat = Cat();
  cat.speak(); // ✅ Cat says: Meow!
}
```

> 🔹 `@override` 不是强制的，但推荐使用
> 🔹 保证方法签名一致，否则会报错

---

### 3️⃣ super 调用父类方法

```dart
class Animal {
  void sleep() {
    print('Animal is sleeping');
  }
}

class Dog extends Animal {
  @override
  void sleep() {
    print('Dog is preparing to sleep...');
    super.sleep(); // 调用父类方法
  }
}

void main() {
  var d = Dog();
  d.sleep();
}
```

输出：

```
Dog is preparing to sleep...
Animal is sleeping
```

---

### 4️⃣ 子类继承构造函数参数

Dart 中可以使用 `super()` 简化父类构造调用。

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Bird extends Animal {
  Bird(super.name); // 直接传递给父类
}

void main() {
  var b = Bird('Parrot');
  print(b.name); // ✅ Parrot
}
```

---

## 🧩 三、封装 + 继承综合示例

```dart
class Vehicle {
  String _brand;
  int _speed;

  Vehicle(this._brand, this._speed);

  // Getter
  String get brand => _brand;

  // Setter
  set speed(int value) {
    if (value < 0) {
      print('Speed cannot be negative!');
    } else {
      _speed = value;
    }
  }

  void move() {
    print('$_brand is moving at speed $_speed km/h');
  }
}

class Car extends Vehicle {
  Car(String brand, int speed) : super(brand, speed);

  @override
  void move() {
    print('🚗 $_brand car is driving at $_speed km/h');
  }
}

void main() {
  var car = Car('Tesla', 120);
  car.move();     // 🚗 Tesla car is driving at 120 km/h
  car.speed = -5; // ❌ Speed cannot be negative!
}
```

> ✅ 这个例子融合了
>
> * 封装（_brand 私有属性 + setter 控制）
> * 继承（Car extends Vehicle）
> * 重写（`move()` 方法）

---

## 📘 小结

| 知识点             | Dart 表达方式   | 示例                               |
| --------------- | ----------- | -------------------------------- |
| 定义私有属性          | `_` 前缀      | `_name`                          |
| Getter / Setter | 控制访问        | `get name` / `set name`          |
| 继承              | `extends`   | `class Dog extends Animal`       |
| 调用父类构造          | `super()`   | `Dog(String name) : super(name)` |
| 方法重写            | `@override` | 重写父类方法                           |
| 封装目的            | 数据安全与控制     | Getter / Setter                  |
| 继承目的            | 代码复用与扩展     | 子类扩展父类功能                         |

---

是否希望我下一步帮你讲 **Dart 的多态（Polymorphism）**？那是继封装和继承之后的第三个核心OOP知识点。
