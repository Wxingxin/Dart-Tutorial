
# ✅ 1. 定义与创建

```dart
// 普通列表
List<int> nums = [1, 2, 3, 4];

// 动态类型列表（可以存放不同类型）
var mixed = [1, 'a', true];

// 空列表
var empty = <String>[];

// 不可修改的常量列表
const fixed = [10, 20, 30];
```


也可以用构造函数：

```dart
var list = List<int>.filled(5, 0); // 长度为5，每个元素初始为0
```

# 🧭 一、基本定义

| 对比点           | 属性（Property）                             | 方法（Method）                                 |
| ------------- | ---------------------------------------- | ------------------------------------------ |
| **是什么**       | 类中存储数据的“特征值”或“状态”                        | 类中定义的“行为”或“功能”                             |
| **是否带括号**     | ❌ 不带括号调用                                 | ✅ 带括号调用（即函数）                               |
| **作用**        | 用来**读取或描述** List 的状态（比如长度、是否为空）          | 用来**执行操作**（比如添加、删除、排序）                     |
| **是否有参数**     | ❌ 没有参数                                   | ✅ 可以有参数                                    |
| **是否会改变List** | 通常不会改变                                   | 通常会改变（除非是纯函数型方法）                           |
| **举例**        | `.length`, `.isEmpty`, `.first`, `.last` | `.add()`, `.remove()`, `.sort()`, `.map()` |

---


📘 观察：

* `list.length` 是一个“值”，它表示当前数组的长度。
* `list.add(4)` 是一个“动作”，执行后数组发生变化。

---

## 🧠 三、Dart 中属性其实是“Getter 方法”

Dart 中的属性不是简单的“变量”，而是通过 **getter** 实现的。
例如 `list.length` 实际上是：

```dart
int get length => _length;
```

也就是说：

> ✅ 属性：`list.length`
> ⛔ 错误写法：`list.length()`（这会报错，因为 length 不是函数）

---

## ⚙️ 四、举个形象的比喻

想象 `List` 是一个“装数据的盒子”。

| 对象     | 含义                                                     |
| ------ | ------------------------------------------------------ |
| **属性** | 盒子本身的状态，例如“盒子里有几个物品”（`length`）、“盒子是不是空的”（`isEmpty`）    |
| **方法** | 对盒子进行操作的行为，例如“往盒子里放一个东西”（`add()`）、“拿掉一个东西”（`remove()`） |

---

## 📋 五、常见属性 vs 方法对比示例

| 属性           | 含义       | 方法         | 含义     |
| ------------ | -------- | ---------- | ------ |
| `length`     | 获取长度     | `add()`    | 添加一个元素 |
| `isEmpty`    | 是否为空     | `remove()` | 删除一个元素 |
| `isNotEmpty` | 是否非空     | `sort()`   | 排序     |
| `first`      | 获取第一个元素  | `clear()`  | 清空     |
| `last`       | 获取最后一个元素 | `map()`    | 遍历映射   |
| `reversed`   | 返回反向迭代器  | `where()`  | 条件过滤   |
| （无）          | —        | `reduce()` | 聚合求和   |
| （无）          | —        | `join()`   | 连接成字符串 |

---




# 🧭 二、数组的属性大全（含解释）

| 属性            | 作用                     | 示例                       |
| ------------- | ---------------------- | ------------------------ |
| `length`      | 返回数组长度                 | `list.length`            |
| `isEmpty`     | 是否为空数组                 | `list.isEmpty`           |
| `isNotEmpty`  | 是否非空                   | `list.isNotEmpty`        |
| `first`       | 第一个元素                  | `list.first`             |
| `last`        | 最后一个元素                 | `list.last`              |
| `reversed`    | 返回一个反转的迭代器（不是List）     | `list.reversed.toList()` |
| `hashCode`    | 哈希值（用于比较）              | `list.hashCode`          |
| `runtimeType` | 数据类型                   | `list.runtimeType`       |
| `iterator`    | 返回可迭代器对象               | `list.iterator`          |
| `single`      | 如果列表只含一个元素，返回该元素，否则抛异常 | `['a'].single`           |

---

## 🧩 三、数组方法大全（功能分类）

---

### 🧱 1. 增加元素相关

| 方法                           | 功能        | 示例                            |
| ---------------------------- | --------- | ----------------------------- |
| `add(value)`                 | 向末尾添加一个元素 | `list.add(5)`                 |
| `addAll(iterable)`           | 添加多个元素    | `list.addAll([6,7,8])`        |
| `insert(index, value)`       | 在指定位置插入   | `list.insert(1, 99)`          |
| `insertAll(index, iterable)` | 在指定位置插入多个 | `list.insertAll(2, [10, 11])` |

📘 **示例：**

```dart
var list = [1, 2, 3];
list.add(4);
list.insert(1, 99);
print(list); // [1, 99, 2, 3, 4]
```

---

### 🧹 2. 删除元素相关

| 方法                        | 功能        | 示例                               |
| ------------------------- | --------- | -------------------------------- |
| `remove(value)`           | 删除第一次出现的值 | `list.remove(2)`                 |
| `removeAt(index)`         | 删除指定下标的值  | `list.removeAt(0)`               |
| `removeLast()`            | 删除最后一个元素  | `list.removeLast()`              |
| `removeRange(start, end)` | 删除指定范围    | `list.removeRange(1,3)`          |
| `clear()`                 | 清空所有元素    | `list.clear()`                   |
| `removeWhere(callback)`   | 根据条件删除    | `list.removeWhere((e) => e > 5)` |

📘 **示例：**

```dart
var list = [1, 2, 3, 4];
list.removeWhere((e) => e.isEven);
print(list); // [1, 3]
```

---

### 🔁 3. 查找与获取元素

| 方法                   | 功能              | 示例                            |
| -------------------- | --------------- | ----------------------------- |
| `contains(value)`    | 是否包含            | `list.contains(3)`            |
| `indexOf(value)`     | 第一次出现的索引        | `list.indexOf(2)`             |
| `lastIndexOf(value)` | 最后一次出现索引        | `list.lastIndexOf(2)`         |
| `elementAt(index)`   | 获取指定索引的值        | `list.elementAt(1)`           |
| `firstWhere(test)`   | 找出第一个符合条件的值     | `list.firstWhere((e)=>e>2)`   |
| `lastWhere(test)`    | 找出最后一个符合条件的值    | `list.lastWhere((e)=>e<5)`    |
| `singleWhere(test)`  | 仅当符合条件的元素有一个时返回 | `list.singleWhere((e)=>e==3)` |

---

### 🔄 4. 遍历与转换

| 方法                     | 功能          | 示例                                     |
| ---------------------- | ----------- | -------------------------------------- |
| `forEach(callback)`    | 遍历元素        | `list.forEach((e)=>print(e))`          |
| `map(callback)`        | 映射为新列表      | `list.map((e)=>e*2).toList()`          |
| `where(test)`          | 过滤符合条件的元素   | `list.where((e)=>e>2).toList()`        |
| `expand(callback)`     | 扁平化嵌套数组     | `[1,2].expand((e)=>[e, e*2]).toList()` |
| `every(test)`          | 所有元素都满足条件   | `list.every((e)=>e>0)`                 |
| `any(test)`            | 是否有任意一个满足条件 | `list.any((e)=>e>3)`                   |
| `reduce(callback)`     | 通过累加器聚合     | `list.reduce((a,b)=>a+b)`              |
| `fold(init, callback)` | 带初始值的聚合     | `list.fold(10, (a,b)=>a+b)`            |

📘 **示例：**

```dart
var list = [1, 2, 3, 4];
var doubled = list.map((e) => e * 2).toList(); // [2, 4, 6, 8]
var sum = list.reduce((a, b) => a + b); // 10
```

---

### 📊 5. 排序与反转

| 方法                | 功能          | 示例                       |
| ----------------- | ----------- | ------------------------ |
| `sort([compare])` | 排序，可自定义比较规则 | `list.sort((a,b)=>b-a)`  |
| `shuffle()`       | 随机打乱        | `list.shuffle()`         |
| `reversed`        | 反转（迭代器）     | `list.reversed.toList()` |

📘 **示例：**

```dart
var list = [3, 1, 4, 2];
list.sort();
print(list); // [1, 2, 3, 4]
```

---

### 📦 6. 子集与复制

| 方法                                   | 功能           | 示例                             |
| ------------------------------------ | ------------ | ------------------------------ |
| `sublist(start, [end])`              | 截取子数组        | `list.sublist(1,3)`            |
| `getRange(start, end)`               | 获取指定范围的迭代器   | `list.getRange(0,2).toList()`  |
| `setRange(start, end, iterable)`     | 替换部分元素       | `list.setRange(1,3,[9,9])`     |
| `fillRange(start, end, [fillValue])` | 用指定值填充       | `list.fillRange(0,2,100)`      |
| `copyRange(...)`                     | 从另一个List复制元素 | `list.setAll(0, [5,6])`        |
| `replaceRange(start, end, iterable)` | 替换指定范围       | `list.replaceRange(1,2,[7,8])` |

---

### 🧮 7. 数组与其他类型转换

| 方法                  | 功能           | 示例               |
| ------------------- | ------------ | ---------------- |
| `join([separator])` | 连接为字符串       | `list.join('-')` |
| `toSet()`           | 转为 Set（去重）   | `list.toSet()`   |
| `toList()`          | 转换为新 List    | `list.toList()`  |
| `asMap()`           | 转为 Map（键为索引） | `list.asMap()`   |

📘 **示例：**

```dart
var list = ['a', 'b', 'c'];
print(list.asMap()); // {0: a, 1: b, 2: c}
```

---

### ⚙️ 8. 修改元素

| 方法                                   | 功能            | 示例                                 |
| ------------------------------------ | ------------- | ---------------------------------- |
| `[]`                                 | 获取/设置元素       | `list[1] = 10;`                    |
| `setAll(index, iterable)`            | 从指定位置开始替换多个元素 | `list.setAll(1, [7,8,9])`          |
| `replaceRange(start, end, iterable)` | 替换范围          | `list.replaceRange(0,2,[100,200])` |

---

### 📋 9. 其他常用方法

| 方法                     | 功能          | 示例                                  |
| ---------------------- | ----------- | ----------------------------------- |
| `cast<T>()`            | 转换类型        | `list.cast<num>()`                  |
| `followedBy(iterable)` | 连接另一个集合（惰性） | `list.followedBy([6,7]).toList()`   |
| `take(n)`              | 取前 n 个      | `list.take(2).toList()`             |
| `skip(n)`              | 跳过前 n 个     | `list.skip(2).toList()`             |
| `takeWhile(test)`      | 取满足条件的前缀    | `list.takeWhile((e)=>e<3).toList()` |
| `skipWhile(test)`      | 跳过满足条件的前缀   | `list.skipWhile((e)=>e<3).toList()` |

---

## 🧠 三、实战案例：功能综合使用

```dart
void main() {
  var list = [1, 2, 3, 4, 5, 6];

  // 过滤偶数并翻倍
  var doubledEvens = list
      .where((e) => e.isEven)
      .map((e) => e * 2)
      .toList();
  print(doubledEvens); // [4, 8, 12]

  // 累加所有元素
  var sum = list.reduce((a, b) => a + b);
  print(sum); // 21

  // 排序 + 反转
  list.sort((a, b) => b.compareTo(a));
  print(list); // [6, 5, 4, 3, 2, 1]
}
```

---

## 🧾 四、总结速查表

| 分类 | 常用方法                                                       |
| -- | ---------------------------------------------------------- |
| 添加 | `add`, `addAll`, `insert`, `insertAll`                     |
| 删除 | `remove`, `removeAt`, `removeLast`, `removeWhere`, `clear` |
| 查找 | `contains`, `indexOf`, `firstWhere`, `lastWhere`           |
| 遍历 | `forEach`, `map`, `where`, `reduce`, `fold`                |
| 排序 | `sort`, `shuffle`, `reversed`                              |
| 子集 | `sublist`, `getRange`, `replaceRange`                      |
| 转换 | `join`, `toSet`, `asMap`                                   |
| 工具 | `take`, `skip`, `followedBy`                               |
